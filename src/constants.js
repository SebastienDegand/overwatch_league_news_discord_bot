const COMMAND_PREFIX = 'owl';
const COMMAND_PING = `${COMMAND_PREFIX} ping`;
const COMMAND_NEXT_MATCH = `${COMMAND_PREFIX} next-match`;

module.exports = {
    COMMAND_PREFIX,
    COMMAND_PING,
    COMMAND_NEXT_MATCH
}